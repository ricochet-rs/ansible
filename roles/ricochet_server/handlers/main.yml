- name: Restart ricochet-server
  ansible.builtin.systemd_service:
    name: ricochet-server.service
    state: restarted
    daemon_reload: true
  # Skip in --check: the unit file may not exist (install task is skipped under --check).
  when: ricochet_mode == "systemd" and not ansible_check_mode
